---
import { urlForImage } from "../utils/urlForImage.js";

export interface Props {
  projectName: string;
  projectLink: string;
  projectThumbnail: object;
}
const { projectName, projectLink, projectThumbnail } = Astro.props as Props;
---

<div class="postwrapper">
  <a href={"/projects/" + projectLink} class="picbgwrapper">
    <p class="">{projectName}</p>
    <img
      class="thumbnail-image"
      src={urlForImage(projectThumbnail).height(600).url()}
      alt={projectName}
      draggable="false"
    />
  </a>
</div>


<style>

 /*  .postwrapper {
    position: relative;
    margin-bottom: 10%;
    border-radius: 4%;
    overflow: hidden;
    box-shadow: 0px 6px 10px -8px var(--dropshadow);
    width: 100%;
    background-color: var(--bgvar);
  }
  .picbgwrapper {
    transition: 0.4s 0.1s ease-in-out;
  }

  .picbgwrapper:hover > .picbg {
    background-size: auto 110%;
    filter: brightness(0.4);
  }

  .postwrapper:hover > .caption {
    opacity: 1;
  }

  .postwrapper:hover > .card-border {
    opacity: 0.6;
  }

  .caption {
    color: white;
    position: absolute;
    left: 50%;
    top: 50%;
    padding: 0 16px;
    margin: 0;
    margin-right: -50%;
    transform: translate(-50%, -50%);
    text-decoration: none;
    font-size: 20px;
    text-align: center;
    z-index: 1;
    opacity: 0;
    font-weight: 600;
    transition: 0.4s ease-in-out;
  } */

  .thumbnail-image {
    width: 100%;
  }

  /* .picbg {
    position: relative;
    padding-top: 100%;
    background-size: auto 100%;
    background-repeat: no-repeat;
    background-position: 50% 50%;
    transition: 0.4s ease-in-out;
  }

  .card-border {
    position: absolute;
    opacity: 0;
    height: 88%;
    width: 88%;
    margin: 6%;
    border-radius: 8%;
    top: 0;
    left: 0;
    border: solid 0.6rem white;
    box-sizing: border-box;
    transition: opacity 0.4s ease-in-out;
  } */
</style>
