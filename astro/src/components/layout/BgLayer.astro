<div class="bg">
  <div class="bg__gradient"></div>
</div>

<style lang="scss">
  .bg {
    position: absolute;
    inset: 0;
    z-index: -999;
    overflow: hidden;
    &::before {
      animation: fade 1s forwards;
      opacity: 0;
      content: "";
      position: absolute;
      inset: 0;
      background-image: url("/noise-light.png");

      -webkit-mask-image: linear-gradient(
        linear-gradient(
          90deg,
          rgba(0, 0, 0, 0.6) 0%,
          rgba(0, 0, 0, 0) 20%,
          rgba(0, 0, 0, 0) 80%,
          rgba(0, 0, 0, 0.6) 100%
        ),
        linear-gradient(
          180deg,
          rgba(0, 0, 0, 0) 0%,
          rgba(0, 0, 0, 0) 70%,
          rgb(0, 0, 0, 0.6) 100%
        )
      );

      mask-image: linear-gradient(
          90deg,
          rgba(0, 0, 0, 0.6) 0%,
          rgba(0, 0, 0, 0) 20%,
          rgba(0, 0, 0, 0) 80%,
          rgba(0, 0, 0, 0.6) 100%
        ),
        linear-gradient(
          180deg,
          rgba(0, 0, 0, 0) 0%,
          rgba(0, 0, 0, 0) 70%,
          rgb(0, 0, 0, 0.6) 100%
        );
    }
    .bg__gradient {
      position: absolute;
      bottom: 0;
      opacity: 1;
      transition: var(--modeswitch-transition-time);

      z-index: -999;
      width: 200%;
      height: 50rem;
      left: 50%;
      background: radial-gradient(
        50% 50% at 50% 50%,
        rgb(var(--clr-wb-light) / 0.2) 0%,
        rgb(var(--clr-wb-light) / 0.1) 30%,
        rgb(var(--clr-transparent)) 100%
      );
      filter: blur(50px);
      transform: translate(-50%, 50%);
      border-radius: 100%;
    }
  }

  body.lightmode .bg .bg__gradient {
    opacity: 0;
  }

  @keyframes fade {
    to {
      opacity: 1;
    }
  }

  @media screen and (width <= 1500px) {
    .bg::before {
      -webkit-mask-image: linear-gradient(
        180deg,
        rgba(0, 0, 0, 0) 0%,
        rgba(0, 0, 0, 0) 70%,
        rgb(0, 0, 0, 0.6) 100%
      );

      mask-image: linear-gradient(
        180deg,
        rgba(0, 0, 0, 0) 0%,
        rgba(0, 0, 0, 0) 70%,
        rgb(0, 0, 0, 0.6) 100%
      );
    }
  }
</style>
