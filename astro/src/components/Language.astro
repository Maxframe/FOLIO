---

---

<div class="language-wrapper">
  <a class="language-option" href="#"> EN</a>
  <span class="language-divider">//</span>
  <a class="language-option current" href="#"> DE</a>
</div>

<style lang="scss">
  .language-wrapper {
    position: absolute;
    display: flex;
    justify-content: center;
    align-items: center;
    top: 0;
    right: 0;
    padding: 1rem var(--content-px);
    height: var(--nav-height);
    padding: 1rem var(--content-px);
    transform: translateX(-3px);
    font-weight: 600;
    gap: 0.5rem;
    z-index: 100;

    .language-option {
      opacity: 0.6;
      color: var(--c-fg);
      transition: var(--modeswitch-transition-time) ease;
      padding: 0.5rem;
      margin: -0.5rem;

      &:hover {
        opacity: 1;
      }
    }

    .language-divider {
      opacity: 0.6;
      transition: var(--modeswitch-transition-time) ease;
    }

    .current {
      opacity: 1;
    }
  }

  @media only screen and (max-width: 800px) {
    .language-wrapper {
      top: var(--nav-height);
      z-index: 98;
    }
  }
</style>
